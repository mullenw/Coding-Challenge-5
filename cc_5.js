//const customer = {name: "Bob", job: "Builder", salary: 100000};

//function formatData(obj){
 //   return `${obj.name}

//let userCount = 0;

//function addUser (username) {
    userCount++
    console.log(`${userName})`)


let nummbers = [1,2,3,4,5]

function addNumbers(numList) {
    return numList.reduce ((sum, n) => sum + n,0)
}

function averageNumbers(fun, numList){
    return fun(numlist) / numList.length
}
let numAvg= averageNumbers(addNumbers, numbers);

console.log(numAvg);